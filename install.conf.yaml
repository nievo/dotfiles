- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/.vim: .vim
    ~/.vimrc: .vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
